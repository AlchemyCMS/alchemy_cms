<%= link_to(
	h(content.atom.file.name),
	download_file_path(:id => content.atom.file.id, :name => content.atom.file.filename),
	:class => "file_link #{content.atom.css_class.blank? ? "" : content.atom.css_class}",
	:title => "#{content.atom.title.blank? ? content.atom.file.filename : content.atom.title}"
) if content.atom.file %>
