<%- unless content.atom.image.nil? -%>
	<%- image_size = content.atom.image_size.blank? ? options[:image_size] : content.atom.image_size -%>
	
	<%- img_tag = image_tag(
		show_image_path(
			:id => content.atom.image.id,
			:name => content.atom.image.urlname,
			:size => image_size,
			:crop => options[:crop],
			:upsample  => options[:upsample],
			:padding => options[:padding],
			:format => (options[:format].blank? ? configuration(:image_output_format) : options[:format])
		),
		:alt => (content.atom.alt_tag.blank? ? nil : content.atom.alt_tag),
		:title => (content.atom.title.blank? ? nil : content.atom.title),
		:class => (options[:css_class].blank? ? nil : options[:css_class]),
		:id => dom_id(content.atom.image)
	) -%>
	
	<%- caption = (content_tag("div", content.atom.caption, :id => "#{dom_id(content.atom.image)}_caption", :class => "image_caption") unless (options[:show_caption] == false)) -%>

	<%- if content.atom.link.blank? -%>
		<%= img_tag %>
		<%- unless content.atom.caption.blank? -%>
			<%= caption %>
		<%- end -%>
	<%- else -%>
		<%= link_to(
			content.atom.caption.blank? || options[:show_caption] == false ? img_tag : img_tag + caption,
			url_for(content.atom.link),
			:title => content.atom.link_title,
			:target => (content.atom.open_link_in_new_window? ? "_blank" : "")
		) %>
	<%- end -%>

<%- end -%>