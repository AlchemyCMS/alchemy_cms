<div class="picture_tool_button">
	<%= delete_with_confirmation_link(
		"",
		_("confirm_to_delete_image"),
		url_for(
			:controller => 'atom_pictures',
			:action => 'destroy',
			:id => atom,
			:options => options
		),
		{
			:title => _("delete_image"),
			:class => "picture_delete"
		}
	) -%>
</div>
<div class="picture_tool_button">
	<%= link_to_wa_window("",
		url_for(
			:controller => 'wa_images',
			:action => 'archive_overlay',
			:molecule_id => atom.molecule,
			:atom_id => atom,
			:swap => true,
			:options => options
		),
		{
			:title => _('swap_image'),
    		:size => '780x450',
    		:resizable => "true"
		},
		:title => _('swap_image'),
		:class => 'swap_picture'
	) %>
</div>
<div class="picture_tool_button">
	<%= link_to_function '', "wa_link_window(this, 420)", :class => (atom.atom.link.blank? ? 'edit_link' : 'edit_link linked'), :title => _('link_image'), :name => "atom_picture_#{atom.id}", :id => "edit_link_#{atom.id}" %>
</div>
<div class="picture_tool_button">
	<%= link_to_function(
		"",
		"$('atom_#{atom.id}_link').value='';$('atom_#{atom.id}_link_title').value='';$('atom_#{atom.id}_link_class_name').value='';$('atom_#{atom.id}_link_target').value='';$('edit_link_#{atom.id}').removeClassName('linked')",
		:class => 'remove_link',
		:title => _('unlink')
	) %>
</div>
<div class="picture_tool_button">
	<%= link_to_wa_window("",
		edit_atom_picture_path(
			:id => atom.atom.id,
	    	:options => options
	    ),
	    {
			:title => _('edit_image_properties'),
	    	:size => (options[:caption_as_textarea] ? (options[:manual_image_size] ? '365x200' : '365x180') : (options[:manual_image_size] ? '365x160' : '365x150'))
		},
		:class => 'edit_picture',
		:title => _('edit_image_properties')
	) %>
</div>
