<div class="<%= atom.atom_type.tableize %>">
	<table border="0" cellspacing="0" cellpadding="0" class="atom_editor_table">
		<tr>
			<td class="atom_editor_atom_type">Typ:</td>
			<td class="atom_editor_atom_name">Name:</td>
			<td>validieren?</td>
		</tr>
		<tr>
			<td><%= atom.atom_type.gsub(/WaAtom/, "") %></td>
			<td>
				<%= text_field(
					"atom",
					"name",
					:value => atom.atom.name,
					:name => "atoms[atom_#{atom.id}][name]",
					:id => "atom_#{atom.id}_name",
					:class => "thin_border"
				) %>
			</td>
			<td style="text-align:center">
				<%= check_box(
					"atom",
					"validate",
					:name => "atoms[atom_#{atom.id}][validate]",
					:id => "atom_#{atom.id}_validate",
					:checked => (atom.atom.validate? ? "checked" : nil)
				) %>
			</td>
		</tr>
	</table>
</div>
