<% form_for @wa_file, :html => {:multipart => true} do |f| %>
	<%= render(
		:partial => "partials/upload_form",
		:locals => {
			:file_types => ['*.*'],
			:file_types_description => _('documents'),
			:model_name => 'wa_file',
			:item_type => _('files')
		}
	) %>
<% end %>
