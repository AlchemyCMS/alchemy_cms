<li>
	<div class="sitemap_left_images">
		<%= render_sitemap_lines(page_for_links, !page_for_links.children.blank?) %>
		<%= render(
			:partial => 'pages/page_status',
			:locals => {
				:page => page_for_links
			}
		) -%>
	</div>
	<div class="sitemap_right_tools">
		<%= link_to_remote(
			"&nbsp;",
			:url => page_molecules_path(page_for_links),
			:method => :get,
			:before => "selectPageForInternalLink('#{page_for_links.id}', '#{@url_prefix + page_for_links.urlname}'); $$('#page_for_links .molecules_for_page').invoke('hide')",
			:loading => "showMoleculesFromPageSelector(#{page_for_links.id})",
			:html => {
				:class => "show_molecules_to_link",
				:title => _('show_molecules_from_page')
			}
		) %>
		<div id="molecules_for_page_<%= page_for_links.id %>" style="display: none" class="molecules_for_page">
			<%= image_tag("ajax_loader.gif", :plugin => "alchemy") %>
		</div>
	</div>
	<div class="wa_sitemap_sitename" id="wa_sitemap_sitename_<%= page_for_links.id %>" name="<%= parse_sitemap_name(page_for_links) %>">
		<%= link_to_function(
			page_for_links.name,
			%(
				selectPageForInternalLink(
					'#{page_for_links.id}',
					'#{@url_prefix + page_for_links.urlname}'
				);
				page_select_scrollbar.scrollTo(
					this.up('.wa_sitemap_sitename')
				)
			),
			:title => (_('page_for_links.choose_page %{name}') % {:name => page_for_links.name}),
			:class => 'wa_sitemap_pagename_link'
		) %>
	</div>
	<%- if !page_for_links.children.empty? -%>
	<ul id="page_<%= page_for_links.id %>_children">
		<%= render :partial => 'page_for_links', :collection => page_for_links.children, :locals => {:area_name => @area_name} %>
	</ul>
	<%- end -%>
</li>
