<div id="wa_flash_notices" style="<%= flash.keys.blank? ? "display: none" : "display: block" %>" >
	<div style="<%= flash[:notice].blank? ? "display: none" : "display: block" %>" class="wa_flash notice">
		<%= flash[:notice] %>
	</div>
	<div style="<%= flash[:error].blank? ? "display: none" : "display: block" %>" class="wa_flash error">
		<%= flash[:error] %>
	</div>
</div>
