<% form_for([:admin, @attachement], :html => {:multipart => true}) do |f| %>
	<%= render(
		:partial => "admin/partials/upload_form",
		:locals => {
			:file_types => ['*.*'],
			:file_types_description => _('documents'),
			:model_name => 'file',
			:item_type => _('files')
		}
	) %>
<% end %>
