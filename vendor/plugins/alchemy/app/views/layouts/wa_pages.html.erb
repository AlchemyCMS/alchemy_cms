<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
	<head>
		<%= render_meta_data %>
		<%= stylesheet_link_tag 'base' %>
		<%= javascript_include_tag "application" %>
	</head>
	<body>
		<div id="page">
			<div id="header">
				Hier zum Beispiel ein <%= link_to "Logo", index_url %>
			</div>
			<div>
				<h1><%= current_page.name %></h1>
				<h2>Language: <%= session[:language] %></h2>
				<h3>Select Language:</h3>
				<% language_codes_for_select.each do |lang|%>
					<%= link_to "#{lang}", index_url(:lang => lang) %>
				<% end %>
			</div>
			<div id="navigation">
				<h2>Hier die Hauptnavi:</h2>
				<%= render_navigation(
						:submenu => true,
						:pagination => {
							:level_2 => {
								:size => 3,
								:current => params[:navigation_level_2_page]
							}
						}) %>
			</div>
			<div id="subnavigation">
				<h2>Hier die Subnavi der aktuellen Seite:</h2>
				<%= render_navigation :from_page => current_page unless root_page? %>
			</div>
			<div id="content">
				Hier alle Moleküle der aktuellen Seite:
				<%= render_page_layout %>
			</div>
		</div>
	</body>
</html>
