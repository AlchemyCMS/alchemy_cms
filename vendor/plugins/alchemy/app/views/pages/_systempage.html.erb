<li>
	<div class="sitemap_left_images">
		<%= render(
			:partial => 'page_status',
			:locals => {
				:page => layoutpage
			}
		) -%>
	</div>
	<div class="sitemap_sitename">
	<% unless layoutpage.locked_for(current_user) -%>
	<%= link_to(
			layoutpage.name, {
				:controller => 'pages',
				:action => 'edit_content',
				:id => layoutpage,
				:layoutpage => true
			},
			:title => _("edit_page"),
			:class => "sitemap_pagename_link"
		) -%>
	<% else -%>
		<%= layoutpage.name %>
	<% end -%>
	</div>
</li>
