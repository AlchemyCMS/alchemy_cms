page.insert_html(
  :bottom,
  "element_area",
  :partial => "element",
  :object => @element
)
Configuration.sortable_elements(page)
WaNotice.show_via_ajax(page, _("successfully_added_element"))
page << "TinymceHammer.init()"
page << "reloadPreview()"
page << "alchemy_window.close();"
page << "if(elements_window){elements_window.updateHeight();}"
page << "main_frame.updateSize();"
page << "scrollToElement('#{@element.id}');"
