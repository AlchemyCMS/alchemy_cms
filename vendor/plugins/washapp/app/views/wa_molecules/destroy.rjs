page.remove("dragable_#{@molecule.id}")
if @wa_page.wa_molecules.empty?
  page << remote_function(:url => {:controller => "wa_molecules", :action => "new", :page_id => @wa_page.id, :open => true})
else
  WaConfigure.sortable_molecules(page)
end
WaNotice.show_via_ajax(page, _("element_deleted_successful"))
page << "reloadPreview()"
page << "setFrameSize()"
