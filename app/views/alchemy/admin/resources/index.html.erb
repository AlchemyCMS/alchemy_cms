<%- label_title = t("Create #{resource_model_name}", :default => t('Create')) -%>

<%- toolbar(
	:buttons => [
		{
			:icon => 'create',
			:label => label_title,
			:url => resource_helper_prefix.send("new_admin_#{resource_model_name}_path"),
			:title => label_title,
			:overlay_options => {
				:title => label_title
			},
			#:if_permitted_to => [:new, namespaced_resources_name.to_sym]
		}
	],
	:search => true
) -%>

<%= render 'table' %>
