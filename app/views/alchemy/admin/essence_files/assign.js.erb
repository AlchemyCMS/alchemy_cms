attachment = <%== Alchemy::AttachmentSerializer.new(@attachment, scope: current_ability).to_json %>;
Alchemy.vuexStore.commit('assignFile', {
  attachment: attachment,
  content_id: <%= @content.id %>,
  element_id: <%= @content.element_id %>
});
Alchemy.closeCurrentDialog();
