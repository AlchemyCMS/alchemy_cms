(function($) {
  Alchemy.ImageCropper.destroy();
  Alchemy.closeCurrentDialog();
  Alchemy.setElementDirty('#element_<%= @content.element.id %>');
<% if @content.ingredient %>
  $('.thumbnail_background img', '#<%= @content.dom_id %>').replaceWith('<%= picture_thumbnail(@picture_style, @options) %>');
  Alchemy.ImageLoader('#<%= @content.dom_id %> .thumbnail_background');
<% end %>
})(jQuery);
