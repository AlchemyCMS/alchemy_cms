<%= toolbar(
	:buttons => [
		{
			:icon => 'upload',
			:url => alchemy.new_admin_attachment_path,
			:overlay_options => {
				:title => t('upload_file', :scope => :alchemy),
				:size => '540x550'
			},
			:title => t('upload_file', :scope => :alchemy),
			:class => 'icon_button',
			:label => t('upload_file', :scope => :alchemy),
			:if_permitted_to => [:new, :alchemy_admin_attachments]
		}
	]
) %>

<%= render :partial => 'files_list' %>
