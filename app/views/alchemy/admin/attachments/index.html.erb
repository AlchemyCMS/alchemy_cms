<%= toolbar(
	:buttons => [
		{
			:icon => 'upload',
			:url => new_admin_attachment_path,
			:overlay_options => {
				:title => _('upload_file'),
				:size => '540x550'
			},
			:title => _('upload_file'),
			:class => 'icon_button',
			:label => _('upload_file'),
			:if_permitted_to => [:new, :alchemy_admin_attachments]
		}
	]
) %>

<%= render :partial => 'files_list' %>
