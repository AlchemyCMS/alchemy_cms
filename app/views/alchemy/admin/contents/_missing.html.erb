<div class="content_editor missing" id="element_<%= element.id %>_content_missing">
  <label><%= I18n.t("content_names.#{element.name}.#{name}", :default => ["content_names.#{name}".to_sym, name.capitalize]) %></label>
  <p>
    <%= render_icon('warning') %>
    <%= t('content_not_found', :scope => :alchemy) %>
    <%= link_to(
      t('create', :scope => :alchemy),
      alchemy.admin_contents_path(
        :content => {
          :element_id => element.id,
          :name => name
        },
        :was_missing => true
      ),
      :method => 'post',
      :remote => true,
      :class => 'button'
    ) %>
  </p>
</div>
