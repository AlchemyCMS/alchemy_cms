<%- content_for(:toolbar) do -%>
<div id="toolbar_buttons">
	<%= toolbar_button(
		:icon => 'upload',
		:label => _('upload_image'),
		:url => new_admin_picture_path(:size => @size),
		:overlay_options => {
			:title => _('upload_image'),
			:size => '540x550'
		},
		:class => 'icon_button',
		:title => _('upload_image'),
		:if_permitted_to => [:new, :alchemy_admin_pictures]
	) %>
	<div class="toolbar_spacer"></div>
	<div class="button_with_label">
		<%= link_to(
			render_icon('zoom-out'),
			admin_pictures_path({
				:size => "small",
				:query => params[:query]
			}),
			:title => _("small_thumbnails"),
			:class => "icon_button please_wait"
		) %>
		<%= link_to(
			render_icon('zoom-equal'),
			admin_pictures_path({
				:size => "medium",
				:query => params[:query]
			}),
			:title => _("medium_thumbnails"),
			:class => "icon_button please_wait"
		) %>
		<%= link_to(
			render_icon('zoom-in'),
			admin_pictures_path({
				:size => "large",
				:query => params[:query]
			}),
			:title => _("big_thumbnails"),
			:class => "icon_button please_wait"
		) %>
		<label><%= _('Image size') %></label>
		<%= hidden_field_tag('size', @size, :id => 'overlay_thumbnails_size') %>
	</div>

	<%- permitted_to?(:flush, :alchemy_admin_pictures) do -%>
	<div class="toolbar_spacer"></div>
	<%= toolbar_button(
		:icon => 'flush',
		:url => flush_admin_pictures_path,
		:overlay => false,
		:link_options => {
			:remote => true,
			:method => :post
		},
		:label => _('Flush picture cache'),
		:class => 'icon_button please_wait',
		:title => _('Flush picture cache'),
		:skip_permission_check => true
	) %>
	<%- end -%>
</div>
<%= render :partial => 'alchemy/admin/partials/search_form' %>
<%- end -%>

<div id="picture_archive">
	<%= render :partial => 'pictures_list' %>
</div>

<%- content_for :javascripts do -%>
<script type="text/javascript" charset="utf-8">

	jQuery(document).ready(function($) {
		Alchemy.inPlaceEditor({
			save_label: "<%= _('save') %>",
			cancel_label: "<%= _('cancel') %>"
		});
	});

</script>
<%- end -%>
