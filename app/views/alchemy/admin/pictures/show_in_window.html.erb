<%= image_tag(
  alchemy.zoom_picture_path(
    :id => @picture.id,
    :name => @picture.urlname,
    :format => :png
  )
) %>
<%= form_for [alchemy, :admin, @picture], :remote => :true do |f| %>
  <%= f.label :tag_list %>
  <%= f.text_field :tag_list %>
  <%= f.submit %>
<% end %>