Alchemy.ImageCropper.destroy();
Alchemy.closeCurrentDialog();
<% if @content.ingredient %>
Alchemy.ImageLoader('[data-content-id="<%= @content.id -%>"] .thumbnail_background');
$('[data-content-id="<%= @content.id -%>"] .picture_image img').attr(
  'src', '<%= j(@content.essence.thumbnail_url) %>'
);
<% end %>
