(function($) {
  
  $('#<%= @container_id -%>').html('<%= escape_javascript(render(
    :partial => "alchemy/elements/<%= @element.name -%>_view.html.erb",
    :locals => {
      :element => @element,
      :options => @options
    }
  )) -%>');
  
})(jQuery);
