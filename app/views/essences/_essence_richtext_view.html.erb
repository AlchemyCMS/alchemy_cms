<%- if options[:shorten_to].blank? -%>
<%= raw content.essence.send(options[:plain_text] ? :stripped_body : :body) %>
<%- else -%>
<%= raw content.essence.send(options[:plain_text] ? :stripped_body : :body).split(" ").first(options[:shorten_to]).join(" ") + (options[:shorten_suffix] || '…') %>
<%- end -%>