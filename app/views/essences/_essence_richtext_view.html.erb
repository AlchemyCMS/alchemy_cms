<%- if options[:shorten_to].blank? -%>
<%= content.essence.send(options[:plain_text] ? :stripped_body : :body) %>
<%- else -%>
<%= content.essence.send(options[:plain_text] ? :stripped_body : :body).split(" ").first(options[:shorten_to]).join(" ") + (options[:shorten_suffix] || '…') %>
<%- end -%>