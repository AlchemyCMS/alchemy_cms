(function($) {
  
  $('#<%= @container_id -%>').html('<%= escape_javascript(render(
    :partial => "elements/<%= @element.name -%>_view.html.erb",
    :locals => {
      :element => @element,
      :options => @options
    }
  )) -%>');
  
})(jQuery);
