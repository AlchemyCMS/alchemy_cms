page.replace 'sitemap', :partial => 'sitemap'
if @sorting
  page.show 'page_sorting_notice'
  page << "jQuery('#page_sorting_button').addClass('active')"
  page << "Alchemy.PageSorter()"
else
  page.hide 'page_sorting_notice'
  page << "Alchemy.pleaseWaitOverlay(false)"
end
