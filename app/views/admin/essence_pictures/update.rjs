page.replace content_dom_id(@content), :partial => "/essences/essence_picture_editor", :locals => {:content => @content, :options => params[:options]}
Alchemy::Notice.show(page, ( _("Image updated successfully") ))
page << "Alchemy.closeCurrentWindow()"
page << "Alchemy.reloadPreview()"
if @content.element.contents.find_all_by_essence_type("EssencePicture").size > 1
  page << "Alchemy.SortableContents('#{form_authenticity_token}')"
end
