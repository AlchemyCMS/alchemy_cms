<%- if page.redirects_to_external? -%>
<%= link_to(
	h(page.name),
	page.urlname,
	:title => (options[:show_title] == true ? page.title : nil),
	:popup => true
) -%>
<%- else -%>
<%- link_path = @preview_mode ? 'javascript:void(0)' : (multi_language? ? show_page_with_language_path(:urlname => page.urlname, :lang => page.language.code) : show_page_path(:urlname => page.urlname)) -%>
<%= link_to(
	h(page.name),
	link_path,
	{
		:class => (page_active?(page) ? 'active' : nil),
		:title => (options[:show_title] ? page.title : nil),
		:lang => page.language_code,
		'data-page-id' => page.id
	}
) -%>
<%- end -%>