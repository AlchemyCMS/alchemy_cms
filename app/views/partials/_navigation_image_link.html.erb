<%= link_to(
	"",
	{
		:urlname => navigation_image_link.urlname
	}.merge(
		{
			:controller => 'pages',
			:action => 'show'
		}
	),
	{
		:class => page_active?(navigation_image_link) ? "active #{navigation_image_link.urlname}" : navigation_image_link.urlname
	}
)%>
