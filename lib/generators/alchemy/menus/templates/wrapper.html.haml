- options ||= {}
- cache wrapper do
  %ul.nav
    = render partial: options[:node_partial_name] || wrapper.node_partial_name,
      locals: { options: options, node: wrapper, render_self: options[:render_self] }
